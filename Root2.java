/**
 * Root2
 */
public class Root2 {

    public static void main(String[] args) {
        char j = 'H';
        int i = 2;
        double r = Math.sqrt(i);

        System.out.format("The sqaure root of %d is %f.%n", i, r);
        System.out.format("%f, %1$+020.10f %n", Math.PI);
    }
}